package amuse;
import java_cup.runtime.*;

parser code
{:
:}

action code
{:
:}

terminal String ifstart, elseclause, elseif, endif, begin, booleano, id, number, parIzq, parDer, cbClose, cbOpen;

non terminal START, EXP, ELSIF;

start with START;

START ::= EXP;

EXP ::= ifstart parIzq booleano parDer begin EXP ELSIF endif;

ELSIF ::= elseif EXP elseclause EXP;
