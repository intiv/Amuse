Main(){

    
    while(hola < 10)
        if(y == 2) then
            while(y==2)
                
            end
        endif
    end
    bool hola(){
        if(a>5) then
        endif
        return hola
    }

    num adios(num first, bool simon, char neles){
        while(asd == 3)
        end
        return 1
    }
}