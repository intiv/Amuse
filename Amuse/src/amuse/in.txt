Main(){
    
    
    while(hola < 10)
        if(y == 2) then
            holiwis bool := 5
            bool simon := true
        endif
    end

    
}

bool hola(){
        if(a>5) then
            adiowis
        endif
        return hola
    }

num adios(num first, bool simon, char neles){
    while(asd == 3)
    end
    return 1
}