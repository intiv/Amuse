num mayor(num, num, num)
void ciclo(num, num)


Main(){
    a num
    b num
    c num
    read(a)
    read(b)
    read(c)
}

num mayor(a num, b num, c num){
    if(a >= b && a>= c) then
        return a
    elseif(b >= a && b >= c) then
        return b
    endif
    return c
}

void ciclo(inicio num, fin num){
    a num
    b num
    read(a)
    read(b)

    while(inicio != fin)
        if(inicio == a)then
            inicio := 2
        elseif(inicio == b)then
            inicio := 1
        else then
            inicio := fin
        endif
    end
    write(inicio)
}